# WL-ARCH-008 Dell r6 connected-session evidence — 2026-08-04

This is evidence for the single active worklist in
`docs/platform/WORKLIST.md`; it is not a parallel tracker. The result qualifies
the current Dell engineering preview only. It does not promote the Browser VM
to production or claim fleet-wide readiness.

## Bound deployment

- Seat: Dell (`DELL-LAPTOP`, `172.20.146.225`)
- Domain: `browser-vm` (`a1100a2f-5b65-4064-ac9f-925e1affa1fb`), autostarted,
  4 vCPU, 8 GiB RAM
- Guest address during proof: `192.168.122.58`
- Image source commit: `1f8bd8450a135546b96525fbd9100f052872e78d`
- Image digest:
  `sha256:17e3820560656d765cb318fb41f81d5dd968b390cb4e58f3ea321602f46b0556`
- Immutable base: `/var/lib/libvirt/images/browser-vm-r5-57e06879.qcow2`
- Writable overlay: `/var/lib/libvirt/images/browser-vm-r5-overlay.qcow2`
- NoCloud session: `session:5d7d3288-351f-4a9a-89b9-f18325546f59`
- r6 seed ISO SHA-256:
  `467f8f036037d219e6b515bf36749067e7eea0b3f24a83757fa7ee44f1b309c4`
- RDP proof source: `9660cbd92c1eeae2d8ccd2d06d2726020da423f4`
- Exact test binary SHA-256:
  `2f31a8182fd6f66860a73ffb3abc407f23925ab89f45c60190886c0c0493bc32`

The r6 seed binds the admitted source, digest, workload identity, session
identity, RDP selection, and `transport_health=connected`. QGA read those exact
values after boot. Every test, reconnect, stop, or deployment mutation was
preceded by Dell's centered red `AI-GENERATED-ALERT` and its enforced five-second
wait. The encrypted host-bound RDP credential was materialized only in process
memory and was absent from logs and command arguments.

## Strict RDP result

Three strict live executions passed during the connected-state qualification
sequence. The latest durable run passed 1/1 tests in 35.19 seconds:

- The real Chromium desktop rendered at exactly 1920x1080 with 43 distinct
  colors and 129,847 non-dominant pixels.
- A primary-pointer click at `(1898,62)` opened Chromium's application menu and
  changed 71,307 pixels in a pointer-anchored region.
- Escape restored 64,601 of 71,307 changed pixels (90.59 percent); the bounded
  residual was 6,706 pixels (9.40 percent).
- Reconnect at the `Compressed` tier repainted 796 pixels per thousand from
  inbound damage and retained 999 pixels per thousand of visual identity.
- The post-reconnect frame contained 1,161 distinct colors.

The credential-free repository log is
`WL-ARCH-008-2026-08-04-dell-r6-live-rdp-9660cbd9.log`, SHA-256
`5cb8a3dfeacbdb256279e1e671fedd2da2e5c59e926ad415ea0b188503eb96c1`.
It differs from Dell's raw artifact SHA-256
`c5b28cfb29d369466ebcb8affa18fd4dcd1b5d10a9100d0ff40fdc6dab7a472f`
only by removal of the extra blank line at EOF.
The three raw 1920x1080 PPM artifacts remain private on Dell because they total
about 18 MiB; their hashes are:

- Settled desktop: `d4cefc674ca066c38e1d3705c4b262ea7e55eb81ca5791e7fb02da9e7c6d42fe`
- Best menu reaction: `6a8fb7871191c267c7d97c791047fef249dbc9fe954f26dcadd92966606981bc`
- Qualification-best frame:
  `613242597be9e33128f2398fb5441c0011b1818e2e6e1895083e930a52aac074`

## Guest runtime and media

The accepted overlay was stopped cleanly immediately after the latest strict
session. Six files were copied read-only from the bootc/OSTree mutable `/var`
before the next boot could invalidate session-scoped claims. Dell then restarted
the VM; QGA was ready after 33 seconds and the domain returned to `running`.

The exact records committed beside this index are:

- `WL-ARCH-008-2026-08-04-dell-r6-runtime.json`, SHA-256
  `53f00f64aa4704af910cf6e2c19e1a1b141bbc3b95b75397ec2b251016505a0e`
- `WL-ARCH-008-2026-08-04-dell-r6-media.json`, SHA-256
  `949d792bb08ce1bcd03ec39aefdfd341fb26d4e15cac916ee33930a9516255a0`

They were streamed directly to isolated farm slot
`.50/browser-r6-evidence`. Both repository validators and both validator
self-tests exited zero. The bounded results are:

- PipeWire 1.6.8, WirePlumber, and Pulse compatibility were live for the actual
  `mcnf-browser` session.
- The VM exposed one stereo 48 kHz playback sink and one stereo 48 kHz capture
  source. This validates endpoint wiring only, not audible playback.
- Guest Chromium decoded the fixed 64x64 VP8/Opus fixture to ready-state 4 with
  four video frames, zero dropped frames, and audio ready-state 4. This validates
  guest-local decode only, not VDI audio presentation.
- `vainfo` found the `virtio_gpu` VA-API driver but initialization failed with
  resource-allocation error code 2. GPU status therefore remains `unavailable`;
  acceptance criterion 7 is not met.

A later bounded GL experiment initialized virgl VA-API but exposed no hardware
decode profile and developed a repeat-boot stall, so it was rolled back. See
`WL-ARCH-008-2026-08-04-dell-r7-gpu-experiment.md`.

The remaining extracted diagnostics stay private on Dell and the isolated farm
slot. Their SHA-256 values are `88a7a2d7...e4512a0` (PipeWire),
`5a92bc61...0f83e9` (VA-API), `660d1e49...a649d0` (media probe), and
`0a97d09b...903de` (runtime log).

## Release disposition

| Gate | Result |
| --- | --- |
| Immutable image, provenance, encrypted login, connected r6 identity | Pass |
| Live Chromium frame, focused pointer input, Escape restoration, reconnect | Pass |
| Guest PipeWire playback/capture endpoint wiring | Pass, bounded claim |
| Guest Chromium fixed-fixture video/audio decode | Pass, bounded claim |
| Guest VA-API hardware acceleration | Fail closed: unavailable |
| Sample-backed stereo playback/capture and recovery | Open; production RDP/WebAudio control hook is not yet installed |
| Five-tab 1080p 15-minute performance and shell latency | Open; live telemetry endpoint is not yet installed |
| Construct auto-selection on released Dell payload | Pass; see the r8 released-shell evidence |
| `.15`, Surface, remaining seats, and six-node rollout | Open |

R1 remains active. The evidence advances the service from transport-only proof
to connected runtime and media-decode proof, while preserving explicit failure
states for GPU, sample-backed audio, performance, released-shell integration,
and fleet rollout.

# WL-CRIT-006 XEN-196 build-farm expansion — 2026-08-08

Physical XEN-196 (`172.20.145.196`) now hosts the dedicated Fedora 42 build VM
`mcnf-build-xen-196` at `172.20.0.196`. The guest has 4 vCPU, 7 GiB RAM, a 60
GiB disk, Rust 1.94.0, and a one-heavy-job cap that preserves capacity for the
unchanged `mcnf-control`, `testvm-lin`, and `testvm-win` guests. The source cloud
image was checked against Fedora's official checksum before import.

The canonical topology, routing helper, build-environment documentation, and
XAPI-native OpenTofu declaration now include the fifth node. The VM is declared
adopt-only because this pool has no golden template. Its exact import command is
documented; state import remains pending because the HTTP backend was not
initialized during provisioning.

## Verification

- `install-helpers/farm-topology.sh table`: 5/5 nodes up and 10 heavy slots.
- `.196`, slot `farm196-mde-bus-r1`: complete `mde-bus` passed 425/425.
- Bash syntax, OpenTofu format/validation, and `git diff --check` passed.

## Destructive operations

The new VM and disk were created on XEN-196. Temporary downloaded cloud-image
files were deleted after provisioning; they are recoverable by redownloading.
No existing guest or repository source was deleted.

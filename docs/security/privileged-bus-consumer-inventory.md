# Privileged Bus consumer inventory

Checked: 2026-08-10. This is an evidence ledger, not a second work tracker.
WL-SEC-007 is now closed and archived in
`docs/worklist-archive/2026-07-24-sec007-closure.md`; the dispositions below
describe the audited code state that supported closure.

## Root and classification rules

The shipped service sets `MDE_BUS_ROOT=/run/mde-bus`. `mde_bus::default_data_dir`
therefore opens that sticky `1777` spool with its cross-UID SQLite index. A
consumer on that root is reachable by any local Bus writer and must treat the
topic as transport, not authority. “Private root” below means the consumer still
uses `dirs::data_dir()/mde/bus` directly (normally
`/root/.local/share/mde/bus`) and is not reachable from the shipped shared spool;
that split is a functional routing defect, not an authorization control.

Auth classes:

- **HMAC v1**: exact canonical body, closed verb/node/target, schema 1, at most
  30 seconds remaining lifetime, and a durable single-use nonce.
- **Typed/confirm only**: parsing, allow-lists, typed echoes, or confirmation,
  but no proof that the shared-spool writer is the privileged shell.
- **Open**: no caller-authentication boundary before the privileged side effect.
- **Open read**: deliberately available list/status/plan/refresh operation.

## Capability-gated or retired

| Exact topic | Production consumer and root | Privileged effect | Current auth class | Legitimate publisher | Disposition |
|---|---|---|---|---|---|
| `action/exec/request` | `workers/action.rs`; shared `/run/mde-bus` | Runs a closed set of administrative lifecycle actions | HMAC v1 | administrative action surface | Covered before this audit; keep hostile no-run tests. |
| `action/cloud/{configure,set-desired,image-build,android-provision,browser-provision,app-provision}` | `workers/cloud`; shared root via `mde_bus::default_data_dir` | Ansible configuration plus typed desired-state, image, Android, Browser, and App mutations | HMAC v1 | Workloads/IaC and specialized typed callers | Auth and routing covered in the 2026-07-23 closure slice; reads stay open. Direct VM provision/lifecycle, container deploy, and console verbs are retired and fail before backend contact. |
| `action/host/<node>/verb`; forwarded `action/host/local/apply` | `workers/host_state.rs`; shared root via `mde_bus::default_data_dir` | Seat volume/display/session/power control | HMAC v1 plus host interlocks | remote host-control surface; local shell consumes the approved apply lane | Auth and routing covered in the 2026-07-23 closure slice; the forwarded lane remains internal and interlocked. |
| `action/pty/<peer>` (`open`, `write`, `resize`, `close`, `detach`, `reattach`) | `workers/pty_broker.rs`; shared `/run/mde-bus` | Opens and drives a root-owned SSH login shell | HMAC v1 via shared `ipc/action_auth.rs` | `mde-term-egui` when hosted by the root shell | Gated in the 2026-07-22 tranche. `heartbeat` and `list` remain open harmless nudges/reads. |
| `action/mesh-mount/<host>` (`mount`, `escalate`, `unmount`) | `workers/mesh_mount.rs`; shared `/run/mde-bus` | Mounts a peer home or full `/` over sshfs; unmounts it | HMAC v1 via shared `ipc/action_auth.rs` | `mde-files-egui` when hosted by the root shell | Gated in the 2026-07-22 tranche. |
| `action/storage/<node>` `apply` | `workers/storage.rs`; shared `/run/mde-bus` (explicitly honors `MDE_BUS_ROOT`) | Applies physical partition/filesystem queues through UDisks/tools | HMAC v1 via shared `ipc/action_auth.rs`, in addition to existing disk walls/typed echo | root shell Storage surface | Gated in the 2026-07-22 tranche; `refresh` remains open. |
| `action/storage/<node>/virtual` `apply` | `workers/virtual_storage.rs`, owned by Storage worker on shared root | Runs `qemu-img`/Podman virtual-storage queues | HMAC v1 via shared `ipc/action_auth.rs` | No production publisher found | Consumer is fail-closed; `refresh` remains open. Add a root publisher only with this envelope. |
| `action/apps/uninstall` | No consumer after the uninstall arm was deleted from `ipc/apps.rs`; shared responder returns unknown verb | Formerly root package removal | Retired | No production publisher found | Deleted rather than carrying an unauthenticated dormant package-removal path; regression test pins its absence. |
| `action/jobs/launch` | `ipc/jobs.rs`; root responder on shared root | Materializes and launches a fleet job | HMAC v1 via shared `ipc/action_auth.rs`, plus a per-target short-lived execution envelope binding run id, node, normalized playbook path, and SHA-256 digest before `ansible-runner` | Root `mackesd remediate fire` CLI, which mints a capability and submits the real Bus request/reply | Bus launch and the CLI publisher share the same gate; the executor refuses unsigned, path-escaped, mismatched, or tampered replicated runs. `list-templates`, `runs`, and `run-results` remain open reads. |
| `action/fleet/{push-revision,rollback,nudge}` | `ipc/fleet.rs`; root responder on shared root | Mutates replicated baseline revisions or triggers convergence | HMAC v1 via shared `ipc/action_auth.rs` | No production Bus publisher found | Gated in the 2026-07-22 tranche; `nudge` additionally restricts signed peer targets to one trimmed non-dot path component before the filesystem write (focused responder suite 9/9). The revision store now admits only validated canonical non-symlink files and commits append-only files with no-replace installation; `magic-fleet` is 50/50 + 3/3, `ipc::fleet` is 9/9, and `netstate_apply` is 3/3. `list-revisions` and `diff-revisions` remain open reads. The direct revisions CLI is a separate authority path. |
| `action/dc/{tofu-apply,tofu-destroy}` | `ipc/tofu.rs`; root responder on shared `/run/mde-bus` | Unattended OpenTofu apply/destroy in allow-listed workspaces | HMAC v1 via shared `ipc/action_auth.rs`, in addition to existing workspace allow-list/confirm checks | No Rust publisher found | Gated in the 2026-07-22 tranche; 14/14 focused farm tests pass. `tofu-plan` and `tofu-state` remain open reads. |
| `action/dc/{host-power,gateway-reboot,dr-backup,lighthouse-restart,lighthouse-promote,host-vlan-create,router-seal-cred,farm-scale,testbed-up,testbed-down}` | `ipc/host_ops.rs`; root responder on shared root | Dom0 power/evacuation, gateway reboot/backup, lighthouse operations, VLAN/credential changes, desired farm shape/plan mutation, and test VM creation/deletion | HMAC v1 via shared `ipc/action_auth.rs`, in addition to existing typed/confirm interlocks | Datacenter shell (where present); several have no Rust publisher | Gated in the 2026-07-22 tranche; 47/47 focused farm tests pass. `host-impact`, `host-pool`, `gateway-status`, `host-net`, `gateway-dhcp`, and `testbed-list` remain open reads. |
| `action/vpn/{add-tunnel,remove-tunnel,tunnel-up,tunnel-down,setup-provider,set-route,clear-route}` | `ipc/vpn_gw.rs`; root responder on shared root | Writes decrypted VPN configs, invokes tunnel tools, changes egress routing | HMAC v1 via shared `ipc/action_auth.rs`, before config/secret-store/backend access | VPN shell surface | Gated in the 2026-07-23 tranche; 26/26 focused farm tests pass, including all seven unsigned mutations and authorized replay. `list-tunnels`, `tunnel-status`, `list-providers`, `list-routes`, `route-status`, `verify-egress`, and `egress-health` stay open. |
| `action/ddns/{set-config,add-record,remove-record}` | `ipc/ddns.rs`; root responder on shared root | Writes replicated DDNS configuration consumed by the egress reconciler | HMAC v1 via shared `ipc/action_auth.rs`, bound to `config` or the normalized record name before config I/O | DDNS shell/CLI surface | Gated in the 2026-07-23 tranche; focused farm coverage is 9/9. `get-config` and `record-status` remain open reads. The direct `mackesd ddns` CLI is an explicit root authority path, not a shared-spool consumer. |
| `action/hardware/surface/{node}/enable` | `surface::enable::SurfaceEnableWorker`; shared root via `mde_bus::default_data_dir` | Runs fixed iptsd/SAM activation and stages the fixed MOK certificate; it has no reboot verb or reboot authority | HMAC v1 via shared `ipc/action_auth.rs`, exact body bound to `surface-enable`, node, and node target, before activation or MOK import | Root-only `mackesd mint-surface-mok-import`; the shell is an observer, not an enable publisher | The short-lived request is bound to a separately sealed, one-use MOK import credential. Any legacy non-null reboot-arm input is refused before effects. Results use the bounded shared `surface_enable` contract and can report `awaiting_governed_host_reboot`, but cannot carry an arm token or `RebootArmed`; the shell navigates to the independent host-state power workflow. |
| `action/hardware/surface/{node}/fw-apply` | `surface::firmware::SurfaceFirmwareWorker`; shared root via `mde_bus::default_data_dir` | Applies one inventory-bound fwupd release to the exact addressed Surface device | HMAC v1 via shared `ipc/action_auth.rs`, exact body bound to `surface-firmware-apply`, node, and fwupd device id before provider access; the separate fixed firmware confirmation phrase must also match | Install/Surface shell publisher | Production uses bounded fixed-argv fwupd inventory and exact device/release/checksum revalidation before device-scoped apply. Missing provider or unavailable release is an honest closed result, not a blanket integration gate. |
| `action/hardware/surface/{node}/camera-proof` | `surface::verify::SurfaceVerifyWorker`; shared root via `mde_bus::default_data_dir` | Opens the fixed libcamera provider for exactly one frame, counts successful completion, and immediately discards it | HMAC v1 exact-body capability bound to `surface-camera-functional-proof`, node, and `one-frame-discard`, plus the fixed operator arm phrase and exact Pro 5/6 generation | Local Surface card | The request/result contracts are bounded and closed. No image bytes, output path, stream, device selector, or retained media crosses the Bus; unsigned, replayed, foreign-generation, unarmed, unavailable-provider, timeout, and capture-failure outcomes fail closed. Fingerprint verification remains separate read-only fprintd device enumeration and never claims a reader or reads enrolled biometric data. |

Consolidated farm evidence for the current tree is green: the shared authorizer
filter passed 2/2, and the cross-consumer `hostile_` filter passed 13/13,
including PTY, mesh-mount, physical/virtual storage, Jobs, and Fleet no-backend
and replay cases.

The 2026-07-23 producer audit closes the previously omitted direct UI writers:
Chat file send-to, Browser session/read-aloud/translate/offline-cache/voice/
passkey/share/send-tab/bookmark, and Chooser desktop-source add/remove now mint
the exact worker-bound capability before writing the shared spool. The Browser
protocol/tab-suspend topics remain projection-only, and `action/voice/dial` has
no in-tree privileged consumer; neither is being promoted to an authority lane
without a consumer effect and a corresponding gate.

## Shared-spool privileged consumers still feeding WL-SEC-007

| Exact topic | Production consumer and root | Privileged effect | Current auth class | Legitimate publisher | Disposition |
|---|---|---|---|---|---|
| `action/dc/{lighthouse-create,genesis-write,sr-create,vdi-create,vdi-attach,vdi-detach,sr-snapshot}` | `ipc/datacenter.rs` plus `ipc/storage_ops.rs`; root responder on shared root | Writes live lighthouse IaC or mutates adopted XCP storage | HMAC v1 via shared `ipc/action_auth.rs`, before op-lock or filesystem/backend calls | shell Datacenter panels | VM lifecycle and console verbs were retired in the Workloads hard cut. Surviving mutations remain gated; `do-regions`, `genesis-plan`, and `backoffice-plan` are open reads. |
| `action/dc/{wol,ipmi-power,idle-policy}` | `ipc/dc_power.rs`; root responder on shared root | Wakes or power-controls hardware and changes idle policy | HMAC v1 via shared `ipc/action_auth.rs` | Datacenter Energy surface | Gated in the 2026-07-23 tranche; 30/30 focused farm tests pass (unsigned/tampered/replayed/future-schema refusal); `wake-eta` is an open read. |
| `action/connect/{set-policy,expose,unexpose,set-template,apply-template}` | `ipc/connect.rs`; root responder on shared root | Changes public exposure and ingress policy | HMAC v1 via shared `ipc/action_auth.rs` | Connect shell surface | Gated in the 2026-07-23 tranche; focused farm suite 12/12. `list-services`, `list-candidates`, and `list-templates` stay open. |
| `action/route/{trace,traceroute}` | `ipc/route.rs`; shared root | Reads exposure/peer/firewall state and optionally runs a bounded local `traceroute` measurement | Open observation/read path; target is restricted to an IP literal or DNS hostname before the root-owned helper runs, with no mutation or authority effect | Route/Network surface | Added in the 2026-07-23 exhaustive audit. The responder remains intentionally open because it returns modeled/measured path data only; malformed, path-shaped, control, and overlong targets degrade to an unavailable typed result. |
| `action/clipboard/{pin,unpin,delete,clear}` | `ipc/clipboard.rs`; root responder on shared root | Mutates/deletes replicated clipboard history | HMAC v1 via shared `ipc/action_auth.rs` | Clipboard surface | Gated in the 2026-07-23 tranche; focused farm suite 2/2. `list` stays open. |
| `action/settings/{set,restore}` | `ipc/settings.rs`; root responder on shared root | Applies host settings or restores a snapshot | HMAC v1 via shared `ipc/action_auth.rs`, before settings I/O | Settings surface | Gated in the 2026-07-23 tranche; focused farm suite 7/7. `get`, `list-keys`, and `snapshot` stay open. |
| `action/voip/{set-gateway,clear-gateway}` | `ipc/voip.rs`; root responder on shared root | Writes/removes replicated VoIP credentials/config | HMAC v1 via shared `ipc/action_auth.rs`, before gateway-file I/O | voice/communications administration surface | Gated in the 2026-07-23 tranche; focused farm suite 6/6. `get-gateway` remains an open read with the password field redacted and a non-secret `password_set` indicator. |
| `action/printers/sync-now` (`list` is a read) | `workers/cups_sync.rs`; shared root | Runs the local CUPS/filesystem convergence and printer import/export commands | HMAC v1 via shared `ipc/action_auth.rs`, bound to the local node before CUPS/filesystem effects | No production publisher found; periodic daemon tick remains an explicit local authority path | Gated in the 2026-07-23 closure slice; unsigned, tampered, authorized, and replayed Bus requests are covered. |
| `action/nebula/regen-certs` | `ipc/nebula.rs`; root responder on shared root | Rotates the mesh CA epoch and peer certificates | HMAC v1 via shared `ipc/action_auth.rs`, before passphrase/CA access | Mesh Control CA-rotation surface | Gated in the 2026-07-23 tranche; focused farm suite 20/20, including exact-body tamper and durable replay refusal. Other Nebula verbs are open reads. |
| `action/file-ops/{send-to,rollback}`; `action/files-inbox/mark-opened`; `action/files-outbox/cancel` | `ipc/files.rs`; root responders on shared root | Copies/rolls back files and mutates inbox/outbox records | HMAC v1 via one process-wide `ipc/action_auth.rs` verifier, before file/inbox/outbox I/O | Files surface | Gated in the 2026-07-23 tranche; focused farm suite 19/19. Listing/audit/download/roster reads stay open. |
| `action/onboard/apply` | `workers/onboard_apply.rs` + `onboard/remote_push.rs`; shared root | Applies a signed role/secret/broker onboarding bundle | Ed25519 issuer authorization, freshness, durable nonce, plus thin-lighthouse media/fileshare scope rejection before apply | Leader onboarding control path | Thin-lighthouse bundle guard added in the 2026-07-23 tranche; focused farm suites are 22/22 and 10/10, including no-partial-apply media-secret refusal. |
| `action/onboard/spawn-lighthouse` | `workers/spawn_lighthouse_onboard.rs`; shared root | Runs the leader-coordinated lighthouse provisioning and CA-migration apply seam | HMAC v1 via shared `ipc/action_auth.rs` for apply; dry-run remains unsigned/read-only | Spawn Lighthouse shell flow | Gated in the 2026-07-23 closure slice; shell publisher and daemon authorization proof is 1/1. Cloud/SSH/CA execution remains an honest integration-gated result when live credentials are absent. |
| `action/seat/remote-input` | `workers/seat_remote_input.rs`; shared root | Invokes the root/uinput helper to inject keyboard and pointer events | HMAC v1 via shared `ipc/action_auth.rs`, bound to node and phone | `workers/kdc_host` | Gated in the 2026-07-23 tranche; focused farm suite 16/16 plus KDC handoff 1/1. |
| `action/seat/remote-input-arm` | `workers/seat_remote_input.rs`; shared root | Changes the local consent/TTL state that permits later phone input and drives the on-seat indicator | HMAC v1 via shared `ipc/action_auth.rs`, exact body bound to `seat-remote-input-arm`, node, and `seat` before arm/disarm state mutation | `mde-shell-egui/seat_remote_input_consent.rs` | Closed in the 2026-07-23 slice: unsigned, tampered, replayed, and hostile arm/disarm controls fail closed; worker proof is 17/17 and shell publisher proof is 10/10. |
| `action/federation/{accept,revoke,refuse-mint}` | `workers/federation_enforcer.rs`; shared root | Changes federation trust/certificate state | HMAC v1 via shared `ipc/action_auth.rs`, before grant/mint/trust I/O | federation UI/control path | Gated in the 2026-07-23 tranche; focused farm suite 6/6, including unsigned-before-state, exact-body tamper/replay, and unsafe-target refusal; shell producer target validation is 1/1 before minting. |
| `action/voice/{provision,did-route,failover,shared-config}` | `workers/voice_provision.rs`; shared root | Provider provisioning and telephony routing/config convergence | HMAC v1 via shared `ipc/action_auth.rs`, before desired-state/provider/config effects; accepted intents persist in owner-only local journal | Communications voice administration | Gated in the 2026-07-23 tranche; focused farm suite 40/40, including unsigned/tamper/replay, restart-safe token-free intent recovery, and group-readable journal refusal. Integration gating remains a backend availability signal, not caller authorization. |
| `action/vehicle/reboot` | `workers/vehicle.rs`; shared root when a gateway is configured | Reboots the attached vehicle gateway over privileged SSH | HMAC v1 via shared `ipc/action_auth.rs` before the ESN probe or SSH, plus typed ESN arming | vehicle control surface | Gated in the 2026-07-23 tranche; focused farm suite 19/19, including unsigned-before-probe, tamper, replay, and authorized typed-arm. `action/vehicle/get-config` stays open read. |
| `action/desktops/{add-source,remove-source}` | `workers/desktop_sources.rs`; shared root via `mde_bus::default_data_dir` | Writes/removes the node-local manual desktop-source store | HMAC v1 via shared `ipc/action_auth.rs`, before store mutation | Desktop Chooser surface | Gated in the 2026-07-23 tranche; focused farm suite 23/23, including unsigned/tampered/replayed requests refused before persistence. `action/desktops/refresh` remains an open read-only discovery nudge. |
| `action/bookmarks/{add,edit,move,delete,add-folder,rename}` | `workers/bookmarks.rs`; shared root via `mde_bus::default_data_dir` | Mutates the root-owned node bookmark op-log and mirrored collection | HMAC v1 via shared `ipc/action_auth.rs`, with typed item/folder/URL targets; daemon-owned `check-links` remains observational | Browser bookmark surface | Gated in the current WL-SEC-007 slice; focused worker suite 15/15. |
| `action/adfilter/{allow,block}` | `workers/adfilter.rs`; shared root via `mde_bus::default_data_dir` | Mutates the root-owned replicated first-party allowlist and compiled filter state | HMAC v1 via shared `ipc/action_auth.rs`, bound to the normalized domain | Browser site-blocking surface | Gated in the current WL-SEC-007 slice; focused tests cover unsigned, tampered, future-schema, oversized, and replay refusal. |
| `action/mesh-transfer/direct` | `ipc/mesh_transfer.rs`; shared root via `mde_bus::default_data_dir` | Runs the typed remote-to-remote SSH/rsync transfer plan | HMAC v1 via shared `ipc/action_auth.rs`, bound to source→destination peers before key/backend access; a root-signed mounted-scope projection is required before any `full` scope is honored | Files `TransferDispatch` plus the root Chat file bridge | The publisher now mints a schema-v1 exact-body `mesh-transfer-direct` capability bound to `src->dst` and fails closed to relay when unavailable; forged, missing, stale, or replayed authority still fails before the transfer plan. |
| `action/mesh/wake` | `ipc/directory.rs`; shared root via `mde_bus::default_data_dir` | Broadcasts a Wake-on-LAN packet | HMAC v1 via shared `ipc/action_auth.rs`, bound to the canonical MAC before `wol::wake` | No production publisher found; CLI wake is a separate explicit authority path | Gated in the current WL-SEC-007 slice; the responder remains fail-closed without a capability. |
| `action/apps/{set-favorite,set-groups}` | `ipc/apps.rs`; shared root via `mde_bus::default_data_dir` | Writes per-user favorites/group files on the shared root | HMAC v1 via shared `ipc/action_auth.rs`; list/favorites/groups/peer-list stay open reads and `launch` is handled by the separately gated peer-app worker | No production publisher found | Gated in the current WL-SEC-007 slice; pure builders remain usable for read/test composition. |
| `action/chat/send` | `workers/chat.rs`; shared root via `MDE_BUS_ROOT` or `dirs::data_dir()/mde/bus` | Mints the local node's Ed25519 signature, appends the message to the node's replicated outbound chat log, and relays `event/chat/message` | HMAC v1 via shared `ipc/action_auth.rs`, exact body bound to `chat-send`, node, and `peer:<to>`/`room:<to>` before signing, persistence, or relay | Root Chat/Browser shell plus Files/Terminal bridges, each minting from the systemd action credential | Closed in the current slice: unsigned, tampered, future-schema, and one-shot replay requests fail before the chat effect; the worker suite is 34/34. Producer bridge suites remain part of the final farm gate. |
| `action/chat/{presence,mute,notify-prefs,room,alert-action}` | `workers/chat.rs`; shared root via `MDE_BUS_ROOT` or `dirs::data_dir()/mde/bus` | Mutates manual presence, seat notification policy, room membership, or typed alert action state/relay | HMAC v1 exact-body capabilities, bound to node and `self`, mute, room, or alert target, are verified before effects | Root Chat shell/render publishers | Gated in the current slice; Chat responder coverage is 34/34 and the shell publisher rerun is farm-green. |
| `action/collab/*` (41 typed Communications commands) | `workers/collab.rs`; universal shared root | Creates and mutates spaces, membership, messages, alerts, clipboard, documents, transfers, calls, and AI-suggestion state | HMAC v1 exact-body capability, bound to `collab-command`, node, and command verb, is verified before deserialization or engine effects | Communications shell command sink | Gated in the current slice; worker hostile/authorized/replay coverage is 16/16 and the Communications publisher proof is 4/4. |
| `action/copilot/ask` | `workers/copilot.rs`; leader-gated shared root | Reads the sealed Codex credential and launches the external `codex` process to answer an operator ask | HMAC v1 exact-body capability, bound to `copilot-ask`, node, and `ask`, is verified before key access or process spawn | Front Door/Copilot ask publisher | Gated in the current slice; focused worker coverage is 58/58, including hostile, authorized, and replay refusal. Proposal/suggestion topics remain proposal/read outputs and never execute directly. |
| `action/music/*` mutable queue/transport/playlist/peer takeover verbs | `services/mde-musicd/bus_responder.rs`; shared root | Changes playback, queues, playlists, or peer-control state | HMAC v1 exact-body capability, target-scoped and single-use, before queue/transport/playlist/peer effects | Music surface | Gated in the current slice; focused responder suite is 21/21, including hostile, authorized, and replay refusal; reads/status remain open. |
| `action/browser-policy/set`, `action/browser/passkey`, browser session/cache/command actions | `mde-browser-workers/{browser_policy,browser_passkeys,browser_session_sync,browser_offline_cache,browser_read_aloud,browser_voice_command,browser_translate}.rs`; shared root | Persists browser policy/credential/session state or triggers configured command execution | HMAC v1 exact-body capability, target-scoped and single-use, before mutation or command execution | Browser worker/UI bridges | Gated in the current slice; the serialized full worker suite is 110/110, including hostile, authorized, and replay refusal; a parallel run exposed only an environment-sensitive STT stdout race. |
| `action/browser/protocol` | `mde-browser-workers/browser_protocol.rs`; shared root | Publishes browser protocol/status projection events; no direct privileged backend effect identified | Open projection lane; no caller-authentication boundary | `mde-shell-egui/web/mod.rs` | Added by the 2026-07-23 exhaustive audit. Keep classified as non-authoritative until a future backend effect is added; if its contract expands, require HMAC v1 before dispatch. |
| `action/browser/tab-suspend` | `mde-browser-workers/browser_tab_suspend.rs`; shared root | Publishes browser tab-suspension/status projection events; no direct privileged backend effect identified | Open projection lane; no caller-authentication boundary | Browser shell worker bridge | Added by the 2026-07-23 exhaustive audit. Keep classified as non-authoritative until a future backend effect is added; if its contract expands, require HMAC v1 before dispatch. |
| `action/connect/share` browser producers | `mde-browser-workers/{browser_session_sync,browser_share}.rs`; shared root | Publishes a connect/share mutation for downstream handling | Direct Browser share/send-tab producers mint the exact-body HMAC v1 envelope with the root credential and target `device:<device_id>` before publication | Browser session/share bridges | Gated in the current slice; the independent phone-share and send-tab producer regressions pass 2/2, including target mismatch refusal. |
| `action/file-ops/send-to` producer | `services/mde-files/bus_backend.rs` plus the root Chat file bridge; shared root | Requests peer file transfer | Producer mints the exact-body HMAC v1 capability with the root credential before publication and fails closed without it | Files transfer backend and Chat `send_file` bridge | Gated in the current slice; the existing focused producer suite is 15/15, and the Chat file bridge now uses the matching `file-ops-send-to` envelope; responder-side file mutation gate is covered elsewhere. |
| `action/compute/migrate` | `workers/compute_migrate.rs`; shared root via `mde_bus::default_data_dir` | Source-side virsh/rsync migration dispatch | HMAC v1 via shared `ipc/action_auth.rs`, before virsh/rsync | Compute migration surface | Source and event dispatch are gated in the 2026-07-23 tranche; focused farm suite 55/55 covers unsigned-before-dispatch, tamper nonce preservation, replay refusal, and unsigned/tampered/replayed ready/committed/failed event rejection. Production publishers now mint distinct schema-v1 HMAC envelopes for all three event lanes before `virsh define/start` or `undefine/rollback`. |

| `action/services/lifecycle-result` | `ipc/directory.rs`; shared root | Reads and deletes a replicated lifecycle result file for a requested peer/id | HMAC v1 via shared `ipc/action_auth.rs`, exact body bound to `lifecycle-result`, local responder node, and `<peer>:<id>` before `take_result` | No production publisher found after the direct lifecycle verb retirement | Closed in the 2026-07-23 audit slice; unsigned, authorized, and durable replay polls are covered before result consumption. |
| `action/schedule/place` | `workers/scheduler.rs`; shared root | Produces placement proposals and desired-state records only; it never invokes a VM/container actuator | Open observation/nudge lane; request parsing is typed, but it is not an authority for lifecycle execution | Scheduler/placement surface | Explicitly non-authoritative. Any future execution must publish the bounded authenticated `action/workload/operation` contract. |
| `action/netassess/refresh` | `workers/netassess.rs`; shared root | Triggers bounded local network telemetry collection between hourly ticks | Open observation/nudge lane; no destructive backend effect identified | Portal network-assessment refresh | Explicitly non-authoritative; command inputs are fixed and the request body is not interpreted. |
| `action/surrounding/refresh` | `workers/surrounding_worker.rs`; shared root | Triggers a surrounding-host discovery sweep | Open observation/nudge lane; no direct privileged mutation identified | Mesh Control surrounding-host refresh | Explicitly non-authoritative; keep this row under review if discovery starts changing firewall/trust state directly. |
| `action/units/get-stream` | `workers/unit_aggregator/verb.rs`; shared root | Returns a typed service/unit stream | Open read | Explorer/Fleet service inventory | Read-only; no caller-authentication boundary is required. |
| `action/editor/open` | `services/mde-files/editor_open.rs`; shared root | Hands a selected file to the local Editor surface | HMAC v1 exact-body capability bound to `editor-open`, local node, and `editor`, before the shell drains the request; no privileged backend effect identified | Files surface | Capability-gated in the current slice; unsigned requests are skipped by the Editor drain while the path remains a local UI projection rather than a host-security authority. |
| `action/browser/media-control/<node>` | `mde-shell-egui/web/mod.rs` plus the KDC MPRIS bridge; shared root | Applies an allow-listed transport command to a local Browser tab | Open local projection lane; KDC origin is pairing-gated and Browser-targeted commands now require an active Browser media projection | Browser media surface and paired-phone MPRIS bridge | Explicitly non-authoritative for host security. The focused KDC regression is 1/1 and the Browser worker suite is 110/110; if media control gains a host/backend effect, require HMAC v1 before dispatch. |

## Production-root splits and dormant/private consumers

These do not reduce the threat of the shared responders above. They explain why
some apparently implemented surfaces cannot reach their worker in the shipped
service and must not be mistaken for a security boundary.

| Exact topic | Private consumer | Effect/auth | Publisher/reachability | Disposition |
|---|---|---|---|---|
| live classified `action/cloud/*` mutations | `CloudWorker` shared resolver | HMAC v1 | Workloads/IaC and specialized typed callers publish shared root | Resolver regression passes. Retired provision is retained only for an explicit no-effect refusal; direct VM lifecycle and console verbs are unclassified. |
| `action/host/<node>/verb` | `HostStateWorker` shared resolver | HMAC v1 | remote host surface targets shared root | Closed in the 2026-07-23 closure slice; authenticated apply-forwarding regression passes. |
| `action/connect/{pair,pair-device,unpair,ring,sms,clipboard,share,sftp,mesh-enroll-token}` | `workers/kdc_host`; shared root via `mde_bus::default_data_dir` | Pairing/store mutation, outbound phone actions, and invite minting | HMAC v1 via shared `ipc/action_auth.rs`, exact `connect-*` context before store/transport/invite effects | Phones hub publisher; read-only roster/browser queries remain open | Gated and routed in the 2026-07-23 closure slice; focused BigBoy suite 63/63 covers unsigned-before-effect, exact-body tamper, and durable replay refusal. |
| `action/apps/launch` | `workers/peer_app_launch.rs`; shared root via `mde_bus::default_data_dir` | Spawns an allow-listed peer app process | HMAC v1 via shared `ipc/action_auth.rs`, before catalog resolution/launch | Peer-app launcher surface | Gated and routed to the shipped shared spool in the 2026-07-23 tranche; focused suite 8/8. |
| `compute/{expose,unexpose}/<peer>` | `workers/compute_expose.rs`; shared root via `mde_bus::default_data_dir` | firewalld forward-port mutation | HMAC v1 via shared `ipc/action_auth.rs`, before firewall calls | Compute shell surface | Gated in the 2026-07-23 tranche; focused farm suite 27/27, covering unsigned, body-tampered, authorized, and replay refusal for both verbs. |
| `action/vdi/session` | `workers/session_broker.rs`; shared root via `mde_bus::default_data_dir` | Session roster/state mutations and leader convergence | HMAC v1 via shared `ipc/action_auth.rs`, before roster/store mutation | VDI session surface | Gated and routed in the 2026-07-23 tranche; focused farm suite 29/29, including unsigned-before-roster, exact-body tamper, and single-use replay. The shell open publisher now emits a session-targeted capability; producer proof is 1/1. |
| `action/vdi/session` (console broker consumer) | `workers/console_broker.rs`; shared root | Resolves a session VM console and starts/stops the local SPICE/VNC relay | HMAC v1 via the console broker's exact session-body verifier before `virsh domdisplay` or relay spawn | VDI session surface | Gated in the 2026-07-23 closure slice; unauthorized session requests are rejected before console discovery. The two-node live VM/console drill remains an owning-epic integration gate. |
| `action/vdi/roaming` | `workers/session_roaming.rs`; shared root via `mde_bus::default_data_dir` | Roaming-session/layout mutations | HMAC v1 via shared `ipc/action_auth.rs`, before fold/layout/store writes | VDI roaming surface | Gated and routed in the 2026-07-23 tranche; focused farm suite 49/49, including unsigned-before-fold, exact-body tamper, and single-use replay. |
| `action/onboard/service-add` | `workers/service_onboard.rs`; shared root via `mde_bus::default_data_dir` | Service onboarding orchestration; live Music lighthouse path is retired and returns a typed refusal | Dry-run is read-only; apply uses HMAC v1 via shared `ipc/action_auth.rs` before resolve/backend dispatch | Services shell Apply mints the root/systemd capability; Preview stays read-only | Gated in the 2026-07-23 tranche; daemon focused suite 14/14 and shell publisher suite 8/8 cover unsigned, tampered, authorized, and replayed apply bodies on the remaining Voice/P2P mutation path. |

## Non-Bus privileged replicated-directory channels

These are not consumers of the cross-UID `/run/mde-bus` spool, so they are not
counted as shared-Bus HMAC rows. They are still privileged mutation authorities
and remain visible here to keep the publisher and transport inventory exhaustive.

| Replicated path | Production consumer and root | Privileged effect | Current auth class | Legitimate publisher | Disposition |
|---|---|---|---|---|---|
| `action/router/<target-host>/*.json` | `workers/router_action.rs`; replicated workgroup root, not the shared Bus spool | Applies typed Vyatta firewall edits to the node's primary gateway | Typed appliance-id confirmation plus node-local gateway binding, hash-chain audit, and `commit-confirm` auto-revert; live mutation additionally requires `MDE_ROUTER_ACTION_LIVE=1` | Device Manager/router-control shell | Separate archived WL-RUN-006 authority path. The default is staged and audited; the live gateway smoke remains operator-gated. This channel is not represented as HMAC v1 and must not be described as closed by WL-SEC-007's shared-spool fixture. |
| `upgrade-intent/*.json` | `workers/upgrade_intent_watcher.rs`; replicated mesh root | Runs coordinated `dnf upgrade`/`mde-install` actions on every daemon watcher | Root-issued exact-body HMAC v1 capability bound to the intent filename and re-issued after each authenticated state transition | `mackesd upgrade --coordinate` | Remediation is farm-proven 16/16: the CLI signs the canonical replicated intent and the watcher refuses unsigned, tampered, expired, replayed, or provenance-mismatched documents before package-manager execution; valid transitions reach the package-manager seams. |
| `node-tags/<host>.json` | `cli/tag.rs`; replicated mesh root | Changes capability tags consumed by job execution and hop/hypervisor duties | Direct CLI/operator authority with single-component target-path validation | `mackesd tag --host … --set …` | Target validation is implemented and farm-proven 2/2; dotted, mixed-case, underscored, and `peer:` mesh identifiers remain accepted while traversal/control/path escapes are rejected. |
| `topology/hops/<host>.json` | `cli/hop_advertise.rs` + `workers/nebula_supervisor.rs`; replicated mesh root | Influences Nebula unsafe-route folding | Direct CLI/operator authority with local-owner, canonical IPv4, and canonical network-CIDR binding | `mackesd hop-advertise` | Owner/IP/CIDR validation is implemented and farm-proven 4/4; mismatched owners, unsafe host keys, unusable/non-local addresses, host-bit routes, and malformed CIDRs are rejected before publication. |
| `trust.json` | `cli/surrounding_trust.rs` + `workers/mesh_firewall.rs`; replicated workgroup root | Folds surrounding-host trust into persistent firewalld DROP rules | Signed Ed25519 schema-v1 envelope, owner/mode-0600 no-follow sealed storage, canonical `<workgroup-root>/surrounding` root, and firewall retain-on-error behavior; direct operator trust remains an explicit non-Bus authority | `mackesd surrounding-trust` | Root-split and forged-entry remediation are closed: surrounding-host tests are 52/52 and mesh-firewall tests are 4/4, including malformed, tampered, symlink, unsafe-permission, and no-unblock cases. |
| `fleet/nudges/<hostname>` | `workers/fleet_reconcile.rs`; replicated workgroup root | Removes the marker and triggers `magic-fleet reconcile`, which can converge host-local state | Authenticated producer envelope is carried through the replicated marker; destination verifies exact-body HMAC v1 and durably consumes its nonce before reconcile | `ipc/fleet.rs` `action/fleet/nudge` responder | Closed in the 2026-07-23 follow-on slice; unsigned/plain markers, wrong-host bodies, and durable replay are refused before reconcile (focused worker proof 1/1). |
| `ssh-keys/{user/}<hostname>.pub` | `workers/ssh_pubkey_gossip.rs`; replicated workgroup root | Installs an enrolled peer's SSH key into the managed `authorized_keys` block | Signed schema-v1 envelope binds the exact `peer:<host>`, user lane, and SSH key tuple; unsigned, tampered, or relocated files are rejected. The embedded node-key signature is provenance, not a new membership ACL: enrolled-mesh auto-trust remains an intentional direct policy and is not HMAC-closed. | `ssh_pubkey_gossip` peer publishers | Closed in the 2026-07-23 exhaustive audit slice: plain replicated keys no longer reach `authorized_keys`; focused farm proof covers valid, unsigned, tampered, wrong-node, and wrong-lane cases. |
| Local Nebula leadership marker consumed by `mirror_syncd` | `workers/nebula_supervisor.rs` → `workers/mirror_syncd.rs`; local/replicated state root | Influences which worker treats this node as the active leadership authority | Exact schema, owner/mode-0600 no-follow sealed marker bound to the local node, plus matching live filesystem/etcd lease; this is a direct authority protocol, not shared-Bus HMAC | Nebula supervisor | Closed in the 2026-07-23 provenance slice: supervisor 33/33 and mirror 3/3; marker existence alone cannot elevate mirror pulls. |
| `nebula-bundle.json`, CA ban/revoke state, enrollment bearer ledger | `cli/ca.rs`, `ca/revoke.rs`, `cli/enroll_token.rs`; replicated mesh root | Issues/revokes certificates, updates ban lists, and mints enrollment bearers | Existing CA/issuer/bearer cryptographic protocols plus no-follow/validated-generation reads and atomic sealed writes | CA/enrollment CLI | Direct authority is now guarded: CA seal hostile-link suite 12/12 and enrollment/endpoint/listener suite 80/80; path-shaped `ca revoke`/`lighthouse retire` identities are rejected before SQLite or replicated-file mutation and revoke events use JSON serialization (focused revocation suite 5/5). The current integrated BigBoy library gate is 3,876 passed, 0 failed, 1 ignored. |
| `/etc/nebula/enroll-endpoint.{crt,key}` | `cli/found.rs` + `workers/nebula_enroll_listener.rs` | Terminates the fingerprint-pinned `/enroll` TLS bootstrap endpoint | Explicit root/operator authority; the private key uses the no-follow, atomic, create-at-0600 sealed writer before listener use | `mackesd found` | The endpoint key no longer uses plain `fs::write` followed by chmod; symlink replacement and transient-unsafe-permission windows are closed by the shared sealed writer. The public certificate remains a non-secret direct bootstrap artifact. |
| `ca/rotation-passphrase.hash` | `ca/rotation_gate.rs`; replicated mesh root | Stores the hash that authorizes CA rotation | Direct CLI authority with the shared no-follow, mode-0600 atomic sealed writer | `mackesd ca set-passphrase` | Closed in the 2026-07-23 slice: hostile leaf replacement and symlinked-parent refusal are covered in 5/5 focused farm tests; the stored value remains a digest, never the passphrase. |
| Local secret store and mesh SSH key/config state | `cli/secret.rs`, `ipc/secret_store.rs`, `ipc/mesh_ssh_key.rs` | Changes encrypted replicated secrets or authorized SSH/sshd configuration | Explicit root/operator authority with validated names, no-follow reads, mode-0600 atomic local writes, and staged SSH configuration replacement | Secret and mesh-SSH CLI | Direct authority is now guarded: secret-store hostile/round-trip suite 12/12 and mesh-SSH provisioning suite 14/14; symlink targets, unsafe names, invalid mesh users/CIDRs, and partial-commit paths are covered. |
| Mirror/image/profile replicated definitions and provisioning artifacts | `workers/mirror_syncd.rs`, `cli/images.rs`, `cli/profiles.rs`; replicated mesh root plus host package/image paths | Changes package repositories, runs image builders, and overrides install metadata | Direct provisioning authority with safe target/content identity checks and atomic durable definition writes | Mirror/image/profile CLI and daemon | Direct authority is now guarded: image catalog suite 9/9 and install-profile suite 13/13; symlinked parents/final targets, moved content, invalid kinds/versions/roles/tags, and replacement durability are covered. |
| `.mackesd-leader.lock` | `cli/take_leadership.rs`; replicated mesh root | Forces the leader identity trusted by leader-gated workers | Explicit operator override with no-follow lock acquisition and shared-root ownership guard | `mackesd take-leadership` | Direct authority is now guarded: leadership suite 15/15, including hostile lock-target refusal and force-take refusal outside the shared writable root. The override remains operator-only and explicitly audited. |

## Explicit direct-CLI authority paths

The shared-spool review does not convert every root-executed CLI into a Bus
capability consumer. A process launched directly as the root service/operator is
already inside the explicit local authority boundary; these paths do not read an
`action/*` spool row and therefore cannot be secured by making the responder
verify a Bus envelope. The audited mutation paths that correspond to shared
responders are:

| Direct command path | Direct effect | Boundary/disposition |
|---|---|---|
| `mackesd remediate fire` → signed `action/jobs/launch` request/reply | Submits a remediation job through the Jobs responder | Root/systemd credential mints the exact HMAC capability, then the real shared Bus responder performs the mutation; no direct `build_reply` bypass remains. |
| `mackesd revisions rollback` and `mackesd fleet-push-setting` | Writes desired-config/fleet revision state directly | Explicit root CLI authority; the Bus fleet mutation gate remains independently required. |
| `mackesd ddns {set-config,add-record,remove-record}` | Writes DDNS config through the same service builder | Explicit root CLI authority; the Bus DDNS responder still requires HMAC v1. |
| `mackesd wake-peer` | Sends a WoL packet directly | Explicit root CLI authority; the Bus `action/mesh/wake` responder remains fail-closed without HMAC v1. |
| `mackesd vpn-import` | Writes a named VPN profile under the shared topology store | Explicit root CLI authority; profile names are restricted to one safe filename component before the shared-root write. Focused path-traversal proof is 1/1. |
| `mackesd validate run` | Writes the leader nudge marker under the shared validation tree | Explicit root CLI authority; the sticky-world-writable marker is opened with `O_NOFOLLOW`/`CLOEXEC` and synced, so a hostile final symlink cannot redirect the write. Focused symlink-redirection proof preserves the outside sentinel. |
| `mackesd node-admin remove-peer` / `lighthouse-retire` | Removes etcd membership and performs lighthouse cleanup before any droplet deletion | Explicit root CLI authority; missing bridge, etcd failure, or cleanup failure now blocks completion. Focused fail-closed proof is 1/1. |
| `mackesd leave --yes` | Evicts the local cert, removes this node's replicated roster/authority rows, wipes `/etc/nebula`, and unpins the local role | Explicit root CLI authority; when configured, etcd membership removal is confirmed before local teardown, and unavailable/failed membership removal leaves local state intact for retry. Focused departure-gate coverage covers confirmed, idempotent, unavailable, and failed outcomes. |

Other CLI commands remain classified as local provisioning, enrollment,
recovery, read-only, or operator-maintenance paths rather than shared-spool
consumers. The rows above are the direct authorities that still need explicit
review; their local root/process boundary does not make their replicated inputs
an implicit security boundary.

## Evidence anchors

- Shared-root contract: `crates/platform/mde-bus/src/lib.rs`.
- Root responder registration and reachability: `crates/mesh/mackesd/src/bin/mackesd/spawn.rs`.
- Common verifier: `crates/mesh/mackesd/src/ipc/action_auth.rs`.
- Capability implementation and durable replay ledger:
  `crates/mesh/mackesd/src/workers/cloud/gate.rs`.
- Consumer source paths and topic constants are named in each table row; the
  apps-uninstall retirement is pinned by `ipc/apps.rs` tests.

This ledger must be rechecked whenever an `action/*` consumer gains a backend,
changes its Bus-root resolver, or adds/removes a verb. Any new work item belongs
under a new or reopened worklist epic, not in this file.

## WL-SEC-007 closure evidence — 2026-07-24

- BigBoy `.130`: the current-tree `mackesd` async library gate passed 3,879
  tests, with zero failures and one ignored test.
- Farm `.90`: the real cross-UID shared-spool fixture passed. An unprivileged
  writer persisted unsigned, tampered, authorized, and replay bodies; only the
  authorized body produced one effect, and replay was refused.
- Live `.15`: an unsigned `action/apps/set-groups` request was refused before
  the app-groups file changed. A root-credential-signed request persisted a
  temporary probe group, a second signed request restored the prior state, and
  the exact pre-probe file state was verified. No secret, token, or credential
  material was recorded.

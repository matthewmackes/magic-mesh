# WL-UX-006 closure

`WL-UX-006` — Construct interface — **Done**. The production Home surface is
icon-free and wallpaper-backed, the Dock is bottom-centered at 64px with 40px
controls and a divider, and the grouped launcher/front-door contracts remain
covered by the shell tests. BigBoy farm verification recorded the full
`mde-shell-egui` suite at 1850/1850; the revised
`install-helpers/verify-shell-pixel-proof.py` self-test and Python compilation
passed on farm slot `wl006-pixel-proof-final-20260728`. Acceptance seat `.15`
produced a valid 1920x1080 DRM capture while Sunshine was active, and the
Construct Home profile passed with artifact sha256
`04034219f0d08cb47b6892ead8b0b0ee2961fc1c40baea1c735689ebdc97885c`.
The `.15` uinput/libinput Dock tap produced DRM touch down/up, egui-frame, and
Dock-action logs. The acceptance host was restored to secure boot-lock defaults
with temporary proof overrides removed and shell, daemon, and network services
active at zero restarts.

Dell `172.20.146.225` deployment was attempted but remains unavailable: direct
and farm-host route audits from `.15`, `.50`, `.90`, `.130`, and `.170` all
returned `No route to host`. This is recorded as external deployment evidence,
not a WL-UX-006 GUI acceptance failure; no Dell deployment is claimed.

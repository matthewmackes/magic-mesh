# Shared-Bus authorization closure — 2026-07-24

> **NOT AN ACTIVE TRACKER.** The active platform worklist is
> `docs/platform/WORKLIST.md`.

### WL-SEC-007 - Authenticate privileged shared-Bus mutation consumers

- Disposition: **DONE 2026-07-24.** The exhaustive current-tree consumer and
  publisher audit found no remaining unauthenticated privileged backend
  mutation. Projection/read lanes remain intentionally open and are not
  authority effects. The epic was removed from the active worklist after the
  integrated, cross-UID, and live closure gates passed.
- Verification: BigBoy `.130` current-tree `mackesd --lib --features
  async-services` passed **3,879/3,879** executed tests, with one ignored and
  zero failures. Farm `.90` passed the real unprivileged shared-spool fixture:
  different-UID writes persisted, unsigned/tampered bodies were refused,
  authorized mutation executed once, and replay was refused.
- Live `.15` verification: over `MDE_BUS_ROOT=/run/mde-bus`, an unsigned
  `action/apps/set-groups` request was refused with the app-groups state
  unchanged. A root-credential-signed request persisted a temporary probe
  group; a second signed request restored the exact prior state. The temporary
  probe left no residual state, and no credential or token material was logged.
- Priority: P0
- Complexity: Epic
- Origin or merged source IDs: 2026-07-22 Codex takeover review of the
  production shared-Bus trust boundary and the subsequent exhaustive
  consumer/publisher audit.

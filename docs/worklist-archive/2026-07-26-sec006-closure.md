# WL-SEC-006 closure - Nebula private-key locality

- Disposition: **DONE-LIVE 2026-07-26.** Nebula enrollment and steady-state
  bundle handling now keep each node's private key local to that node, sign only
  requester-owned public keys, reject mutable relay-authority substitution
  against a root-local pin, and have live `.15` rotation/reconnect/prune plus
  blocklist render/reload proof.

## Evidence

- Public-key-only signing path: `crates/mesh/mackesd/src/ca/sign.rs` signs the
  requester's submitted public key and the regression suite proves no peer
  private-key staging remains.
- Public replicated bundle schema: `crates/mesh/mackesd/src/ca/bundle.rs` and
  `crates/mesh/mackesd/src/nebula_enroll_client.rs` reject secret-bearing bundle
  fields such as `peer_key_pem`, `ca_key_pem`, relay-authority private material,
  and lighthouse secret payloads.
- Local relay-authority pin: `crates/mesh/mackesd/src/ca/bundle.rs` creates and
  enforces the root-local pin, while
  `crates/mesh/mackesd/src/workers/nebula_supervisor.rs` rejects replicated
  authority mismatches.
- Live `.15` rotation/reconnect/prune proof:
  `/var/tmp/wl-sec-006-rotation-before-20260726T020302Z` compared cleanly against
  `/var/tmp/wl-sec-006-rotation-after-20260726T020621Z`. The proof showed changed
  active generation/cert/key hashes, pruned stale generations, active Nebula,
  overlay `10.42.0.5`, reachable probe `10.42.0.1`, and a bounded replicated
  secret-marker scan with zero hits.
- Live `.15` post-cleanup proof:
  `/var/tmp/wl-sec-006-post-cleanup-20260726T021300Z` showed one active
  generation, `identity_root_mode=700`, `active_key_mode=600`, `nebula_active`,
  `mackesd_active`, `overlay_ipv4=10.42.0.5`, `probe_reachable=true`, and
  replicated scan `5335` files / `0` hits / not truncated.
- Live `.15` blocklist render/reload proof:
  `/var/tmp/wl-sec-006-blocklist-proof-confirm-20260726T0348Z` proved the
  superseded fingerprint
  `51c8f646ec57ecf946194c8aafa9a95e051c4bceebf09919b227e0b4f33f6901` appears in
  replicated `ca/blocklist` and rendered `/etc/nebula/config.yaml`
  `pki.blocklist`, while the active fingerprint is different, Nebula and
  `mackesd` are active, overlay probe succeeds, and
  `nebula_reload_after_blocklist=true`.
- Final validation before archive: `install-helpers/lint-worklist.sh --self-test`,
  `install-helpers/lint-worklist.sh docs/platform/WORKLIST.md`,
  `install-helpers/verify-nebula-rotation-evidence.sh --self-test`, the synced
  `.50` proof-helper gate, focused Nebula blocklist/supervisor farm gates, and
  `git diff --check` were green during the closure tranche.

# Worklist drain — 2026-07-20

Operator goal: "Drain the worklist by running multiple agents and making full use of
the farm." Executed against `origin/master` `0844ad56` on branch
`agent/worklist-drain-20260720`. Reconcile-first (markers deferred to
`DRAIN-RECONCILIATION-2026-07-19.md`), then fanned the two genuinely-autonomous units
across the two live farm nodes (BigBoy `.130` + F44 builder `.131` down, `.170`
disk-full — farm degraded to `.50`/`.90`).

## Landed (code, farm-green, integrated 35/35)

- **2 red shell tests fixed** (`cc6fe4ee`) — `Surface::Communications` (WL-FUNC-011) was
  in the `Surface` enum but unwired: added to `surface_by_name` (`toast_bridge.rs`, so
  `reach1_every_surface_has_a_goto_verb` resolves it) and moved to the true arm of
  `surface_needs_remote_sessions_fallback` (`main.rs`, it mounts via plain `show()` with
  no own menubar). Full 1608-test suite green.
- **WL-ARCH-006 CloudReply rich-payload decode** (`72159c31`) — `iac/images.rs` decodes
  the `ImageRow` roster; console-attach decodes `ConsoleEndpoint` into an honest console
  section across the desktop/app/android delivery views (mirrors `configure.rs`'s
  `WireCloudReply`). 33 iac tests green. Full VDI-paint (a separate `main.rs` VdiState
  subsystem) is out of scope; the endpoint now surfaces honestly rather than
  "pending decode". Closes WL-ARCH-006's last autonomous remainder.

## Reconciled — 4 active Epics → `Blocked` (gated), 0 `Remaining`

All four active Epics were CODE/PARITY-COMPLETE with only hardware/operator/live-infra-
gated verification left, so their `Remaining` markers were drift. Reclassified to
`Blocked` with the gate named (per the `Blocked` vocabulary):

- **WL-ARCH-001** (OpenStack removal) — CODE-COMPLETE; only the live local-libvirt
  provision+configure smoke (`MDE_CLOUD_APPLY=1`) remains → hardware-gated. Surface
  successor is WL-ARCH-006.
- **WL-ARCH-006** (Workloads cockpit) — CODE-COMPLETE incl. the CloudReply decode landed
  this session; only the live-seat `.15` provision→configure→console→destroy smoke →
  hardware-gated.
- **WL-RUN-003** (Lighthouse full/equal join + add/retire) — CODE-COMPLETE (typed
  add/retire, etcd voter membership, CA inheritance, quorum drain-gate, worker + UI); only
  the live multi-lighthouse fleet add-retire-add drill + DO creds → live-infra gated.
- **WL-FUNC-011** (Communications suite) — PARITY-COMPLETE (full stack + all 6 modes
  live); only the one-big cutover (retire Chat/Voice/Editor/Files) after operator signoff
  → operator-gated.

## Net

Worklist drained to its honest autonomous ceiling: the two real autonomous units landed
green; every remaining item is genuinely gated on hardware, a live fleet, or operator
signoff, and is marked `Blocked` with that gate named — not `Remaining`.

# Rust build artifacts
/target/
**/target/
*.rs.bk

# OS / editor
.DS_Store
*.swp

# Claude Code runtime artifacts (session-local; not source)
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json

# Accuracy/preview harness output (generated screenshots; tests/accuracy/refs/ is committed)
tests/accuracy/captures/

/rpmbuild/
# Generated release artifacts — the Mackes-Carbon icon theme build + its verify
# render output; the versioned tarball ships as a GitHub release asset, not in git.
/dist/
.claude/.drain-active
.claude/settings.local.json
.claude/hooks/drain-stop-guard.sh

# Claude Code session workflow scripts (one-off, local paths) — not source
.claude/workflows/
target-f43/

# BIRTHRIGHT-2 — bundled air-gapped first-boot blobs (ntfy, starship). Fetched +
# sha256-verified at build time by install-helpers/vendor-birthright-blobs.sh and
# shipped in the RPM; third-party binaries, not source.
/vendor/birthright/
install-helpers/__pycache__/

# cp to env.sh (gitignored); source before tofu. Password stays off-repo.
export TF_VAR_xapi_host="https://172.20.145.193"   # KVM-XCP1 test bed dom0
export TF_VAR_xapi_username="root"
export TF_VAR_xapi_password="$(cat /root/.mcnf-xapi-cred)"

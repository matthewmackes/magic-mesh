---
galaxy_info:
  role_name: cloud_vm
  namespace: magic_mesh
  author: magic-mesh
  description: >-
    Configure a cloud-provisioned mesh VM (WL-ARCH-001 Phase B) by driving the
    existing mackesd-written /etc/mackesd/site.yml convergence (SETUP-7). The
    OpenTofu cloud root provisions the VM + its cloud-init mesh-join; this role is
    the Ansible configure leg.
  license: proprietary
  min_ansible_version: "2.12"
  platforms:
    - name: Fedora
      versions:
        - all
dependencies: []

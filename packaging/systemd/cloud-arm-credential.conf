[Service]
# Host-bound ciphertext; systemd decrypts this into a private, read-only
# per-service credential directory and exports only its path.
LoadCredentialEncrypted=cloud-arm-key:/etc/credstore.encrypted/cloud-arm-key

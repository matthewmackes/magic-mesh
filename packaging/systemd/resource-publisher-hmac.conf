[Service]
# Host-bound ciphertext. systemd decrypts the approved
# resource/publisher-hmac secret into the shell's private, read-only
# credential directory; the publisher key never enters the unit file.
LoadCredentialEncrypted=resource-publisher-hmac:/etc/credstore.encrypted/resource-publisher-hmac

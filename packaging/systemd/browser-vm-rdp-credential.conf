[Service]
# Host-bound ciphertext. systemd decrypts this into the shell service's private,
# read-only credential directory; the guest password never enters the unit file.
LoadCredentialEncrypted=browser-vm-rdp:/etc/credstore.encrypted/browser-vm-rdp

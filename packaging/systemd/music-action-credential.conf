[Service]
# The root DRM shell is the only process that receives the Music signing seed.
# mde-musicd is a user service and verifies the corresponding public key from
# /etc/mde/music-action-public-key instead.
LoadCredentialEncrypted=music-action-private-key:/etc/credstore.encrypted/music-action-private-key

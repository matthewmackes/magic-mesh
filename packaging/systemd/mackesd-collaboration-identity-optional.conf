[Unit]
# Compute and observation advertise KVM/Workloads. A missing Collaboration
# identity dest must fail closed for authenticated publication, not take the
# virt stack down with Requires=.
Wants=mcnf-collaboration-identity.service
After=mcnf-collaboration-identity.service

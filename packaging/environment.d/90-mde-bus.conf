# MCNF — point the user session (cosmic GUIs: workbench/applet/wallpaper)
# at the SHARED bus spool the root mackesd daemon uses, so their bus RPCs reach
# the daemon responders. systemd user manager reads this on session start.
MDE_BUS_ROOT=/run/mde-bus
